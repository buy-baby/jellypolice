<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>글 작성</title>
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="/css/header.css">
</head>
<body>
  <%- include("../partials/header") %>

  <div class="container">
    <h2 style="margin-top:0;">자유게시판 글 작성</h2>

    <% if (error) { %>
      <div style="color:#d91a1a; font-weight:800; margin-bottom:12px;"><%= error %></div>
    <% } %>

    <form method="POST" action="/board/write" enctype="multipart/form-data" class="board-form">
      <input name="title" placeholder="제목" required value="<%= (form.title || '') %>">
      <textarea name="content" placeholder="내용" rows="10" required><%= (form.content || '') %></textarea>

      <div class="board-upload">
        <div style="font-weight:800;">이미지 업로드 (최대 5장, 이미지만)</div>
        <input type="file" name="images" accept="image/*" multiple>
      </div>

      <div style="display:flex; gap:10px;">
        <button type="submit" class="board-btn">등록</button>
        <a href="/board" class="board-btn board-btn--ghost">취소</a>
      </div>
    </form>
  </div>
</body>
</html>
