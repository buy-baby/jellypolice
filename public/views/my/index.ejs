<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <title>마이페이지</title>
  <link rel="stylesheet" href="/css/mypages.css" />
</head>
<body>
  <%- include("../partials/header") %>

  <div class="wrap">
    <div class="card">
      <h1 class="h1">마이페이지</h1>

      <div class="info">
        <div class="label">닉네임</div>
        <div class="value"><%= (me && me.nickname) ? me.nickname : "-" %></div>

        <div class="label">아이디</div>
        <div class="value"><%= (me && me.username) ? me.username : "-" %></div>

        <div class="label">권한</div>
        <div class="value"><%= (me && me.role) ? me.role : "user" %></div>

        <div class="card">
            <h2>디스코드 연동 정보</h2>

            <% if (me && me.discord_id) { %>
            <div class="info">
                <div class="label">연동 상태</div>
                <div class="value">연결됨</div>

                <div class="label">Discord ID</div>
                <div class="value"><%= me.discord_id %></div>

                <div class="label">Discord 이름</div>
                <div class="value"><%= me.discord_name || "알 수 없음" %></div>

                <div class="label">마지막 인증</div>
                <div class="value"><%= formatKST(me.discord_last_verified_at) %></div>
            </div>
            <% } else { %>
            <p>디스코드가 연결되어 있지 않습니다.</p>
            <% } %>
        </div>


      <div class="btnRow">
        <a class="btn" href="/my/inquiry" style="margin-right:10px;">내 민원실</a>
        <a class="btn" href="/my/suggest">내 건의실</a>
      </div>
    </div>
  </div>
</body>
</html>
