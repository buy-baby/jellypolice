<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <title>마이페이지</title>
  <link rel="stylesheet" href="/css/mypages.css" />
  <link rel="stylesheet" href="/css/header">
</head>
<body>
  <%- include("../partials/header") %>

  <div class="wrap">
    <h1 class="h1">마이페이지</h1>

    <!-- 계정 정보 박스 -->
    <div class="card">
      <div class="detail-title">계정 정보</div>

      <div class="info">
        <div class="label">닉네임</div>
        <div class="value"><%= (me && me.nickname) ? me.nickname : "-" %></div>

        <div class="label">고유번호</div>
        <div class="value"><%= (me && me.uniqueCode) ? me.uniqueCode : "-" %></div>

        <div class="label">아이디</div>
        <div class="value"><%= (me && me.username) ? me.username : "-" %></div>

        <div class="label">권한</div>
        <div class="value"><%= (me && me.role) ? me.role : "user" %></div>
      </div>
    </div>

    <!-- 디스코드 정보 박스 -->
    <div class="card" style="margin-top:16px;">
      <div class="detail-title">디스코드 연동 정보</div>

      <% if (me && me.discord_id) { %>
        <div class="info">
          <div class="label">연동 상태</div>
          <div class="value">연결됨</div>

          <div class="label">Discord ID</div>
          <div class="value"><%= me.discord_id %></div>

          <div class="label">Discord 이름</div>
          <div class="value"><%= me.discord_name || "-" %></div>

          <div class="label">마지막 인증</div>
          <div class="value"><%= formatKST(me.discord_last_verified_at) %></div>

          <div class="btnRow" style="margin-top:12px;">
            <a class="btn" href="/auth/discord/refresh">디스코드 정보 갱신</a>
          </div>

        </div>
      <% } else { %>
        <div class="empty">디스코드가 연결되어 있지 않습니다.</div>
      <% } %>
    </div>

    <!-- 버튼 2개 -->
    <div class="btnRow">
      <a class="btn" href="/my/inquiry">내 민원실</a>
      <a class="btn" href="/my/suggest">내 건의실</a>
    </div>

  </div>
</body>
</html>
