<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <title>나의 민원</title>
  <link rel="stylesheet" href="/css/style.css" />
</head>
<body>
  <%- include("../partials/header") %>

  <main style="max-width: 900px; margin: 40px auto; padding: 0 16px;">
    <h1 style="text-align:center;">나의 민원</h1>

    <% if (!complaints || complaints.length === 0) { %>
      <p style="text-align:center; margin-top: 24px;">작성한 민원이 없습니다.</p>
    <% } else { %>
      <ul style="margin-top: 24px; line-height: 1.9;">
        <% complaints.forEach(function(c){ %>
          <li>
            <a href="/my/inquiry/<%= c.id %>">
              <strong>#<%= c.id %></strong>
              <span style="margin-left:8px;">
                <%= (c.content || "").slice(0, 40) %><%= (c.content || "").length > 40 ? "..." : "" %>
              </span>
              <span style="margin-left:10px; color:#666; font-size:13px;">
                (<%= (c.created || "").replace("T"," ").slice(0,19) %>)
              </span>
            </a>
          </li>
        <% }) %>
      </ul>
    <% } %>
  </main>
</body>
</html>
