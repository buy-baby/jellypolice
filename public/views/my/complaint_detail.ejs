<!doctype html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <title>나의 민원 상세</title>
  <link rel="stylesheet" href="/css/mypages.css" />
  <link rel="stylesheet" href="/css/header">
</head>
<body>
  <%- include("../partials/header") %>

  <main class="wrap">
    <div class="card">
      <h1 style="display:flex;align-items:center;gap:10px;">
        나의 민원 상세
        <span class="status-badge status-<%= (complaint.status || "미접수").replace(/\s/g, "") %>">
          <%= complaint.status || "미접수" %>
        </span>
      </h1>


      <div class="info">
        <div class="label">이름</div>
        <div class="value"><%= complaint.name || "-" %></div>

        <div class="label">고유 번호</div>
        <div class="value"><%= complaint.identity || "-" %></div>
      </div>

      <div class="content"><%= complaint.content || "" %></div>

      <div class="btnRow">
        <a class="btn" href="/my/inquiry">목록으로</a>
      </div>
    </div>
  </main>
</body>
</html>
