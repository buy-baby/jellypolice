<!doctype html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <title>나의 민원 상세</title>
  <link rel="stylesheet" href="/css/apply_conditions.css" />
  <style>
    .wrap { max-width: 980px; margin: 40px auto; padding: 0 18px; }
    .card{
      background:#fff; border:1px solid rgba(0,0,0,.08);
      border-radius:16px; padding:22px;
      box-shadow: 0 10px 26px rgba(0,0,0,.08);
    }
    .h1{ font-size: 32px; margin: 0 0 16px; text-align:center; }
    .info{ display:grid; grid-template-columns: 140px 1fr; gap:10px 14px; margin: 10px 0 18px; }
    .label{ color:#6b7280; font-weight:700; }
    .value{ color:#111827; font-weight:700; }
    .content{
      background:#f7f9ff; border:1px solid rgba(26,75,217,.12);
      border-radius:14px; padding:16px;
      white-space: pre-wrap; line-height:1.7;
    }
    .btnRow{ display:flex; justify-content:center; margin-top: 18px; }
    .btn{
      display:inline-flex; align-items:center; justify-content:center;
      padding:12px 18px; border-radius:12px;
      background:#1a4bd9; color:#fff; text-decoration:none; font-weight:800;
      box-shadow: 0 10px 24px rgba(26,75,217,.25);
    }
    .btn:hover{ filter: brightness(0.95); }
  </style>
</head>
<body>
  <%- include("../partials/header") %>

  <main class="wrap">
    <div class="card">
      <h1 style="display:flex;align-items:center;gap:10px;">
        나의 민원 상세
        <span class="status-badge"><%= complaint.status || "접수 중" %></span>
      </h1>

      <div class="info">
        <div class="label">이름</div>
        <div class="value"><%= complaint.name || "-" %></div>

        <div class="label">고유 번호</div>
        <div class="value"><%= complaint.identity || "-" %></div>
      </div>

      <div class="content"><%= complaint.content || "" %></div>

      <div class="btnRow">
        <a class="btn" href="/my/inquiry">목록으로</a>
      </div>
    </div>
  </main>
</body>
</html>
