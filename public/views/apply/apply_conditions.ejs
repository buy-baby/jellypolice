<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <title><%= data?.title || "젤리 경찰청 채용 안내" %></title>
  <link rel="stylesheet" href="/css/intro.css" />
  <link rel="stylesheet" href="/css/apply_conditions.css" />
</head>
<body>
  <%- include("../partials/header") %>

  <div class="apply-wrap">
    <div class="apply-title"><%= data?.title || "젤리 경찰청 채용 안내" %></div>

    <div class="apply-grid">
      <!-- 카드 1 -->
      <section class="card">
        <div class="card-head head-green">
          <%= data?.cards?.eligibility?.title || "지원 자격 안내" %>
        </div>
        <div class="card-body">
          <%= data?.cards?.eligibility?.content || "※ 세부 내용은 관리자 페이지에서 수정 가능합니다." %>
        </div>
      </section>

      <!-- 카드 2 -->
      <section class="card">
        <div class="card-head head-orange">
          <%= data?.cards?.disqualify?.title || "지원 불가 사유" %>
        </div>
        <div class="card-body">
          <%= data?.cards?.disqualify?.content || "※ 세부 내용은 관리자 페이지에서 수정 가능합니다." %>
        </div>
      </section>

      <!-- 카드 3 -->
      <section class="card">
        <div class="card-head head-blue">
          <%= data?.cards?.preference?.title || "지원 우대 사항" %>
        </div>
        <div class="card-body">
          <%= data?.cards?.preference?.content || "※ 세부 내용은 관리자 페이지에서 수정 가능합니다." %>
        </div>
      </section>

      <!-- 오른쪽 버튼 -->
      <aside class="side">
        <a class="btn btn-link"
           href="<%= data?.side?.linkUrl || "https://forms.gle/D7zC9H99P6jVXHzo8" %>">
          <%= data?.side?.linkText || "지원서 작성" %>
        </a>

        <a class="btn btn-back" href="/">
          메인으로 돌아가기
        </a>
      </aside>
    </div>
  </div>
</body>
</html>
