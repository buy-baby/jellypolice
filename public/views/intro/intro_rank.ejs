<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <title><%= data.title %></title>
</head>
<body>

<h1><%= data.title %></h1>

<% data.ranks.forEach(group => { %>
  <h2><%= group.group %></h2>
  <ul>
    <% group.items.forEach(item => { %>
      <li><%= item %></li>
    <% }) %>
  </ul>
<% }) %>

</body>
</html>
  