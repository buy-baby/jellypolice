<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <title>건의 목록</title>
  <link rel="stylesheet" href="/css/admin.css">
  <link rel="stylesheet" href="/css/admin_header.css">
</head>

<body>
  <%- include("../partials/admin_header") %>

  <div class="admin-container">
    <div class="admin-page-title">
      <span class="admin-page-icon">💡</span>
      <h1>건의 목록</h1>
    </div>

    <div class="admin-page-actions">
      <a href="/admin" class="back-btn">← 관리자 메인</a>
    </div>

    <% if (!suggestions || suggestions.length === 0) { %>
      <div class="admin-empty">등록된 건의가 없습니다.</div>
    <% } else { %>
      <div class="admin-cards">
        <% suggestions.forEach((s) => { %>
          <a class="admin-card" href="/admin/suggest/view/<%= s.id %>">
            <div class="admin-card-top">
              <div class="dot"></div>
              <div class="admin-card-title">건의 #<%= s.id %></div>
              <div class="admin-card-date"><%= s.created ? s.created.substring(0,10) : '' %></div>
            </div>

            <div class="admin-card-meta">
              <div class="row">
                <span class="k">이름</span>
                <span class="v"><%= s.name || '—' %></span>
              </div>
              <div class="row">
                <span class="k">내용</span>
                <span class="v ell"><%= s.content || '—' %></span>
              </div>
            </div>

            <div class="admin-card-bottom">
              <span class="admin-card-link">보기 →</span>
            </div>
          </a>
        <% }) %>
      </div>
    <% } %>
  </div>
</body>
</html>
