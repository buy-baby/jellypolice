<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8"/>
  <link rel="stylesheet" href="/style.css"/>
  <title>민원 상세보기</title>
</head>

<body>
<header>
  <img src="/logo.png" />
  <h1>민원 상세보기</h1>
</header>

<div class="container">

  <table class="table">
    <tr>
      <th>ID</th>
      <td><%= c.id %></td>
    </tr>

    <tr>
      <th>이름</th>
      <td><%= c.name %></td>
    </tr>

    <tr>
      <th>고유번호</th>
      <td><%= c.identity %></td>
    </tr>

    <tr>
      <th>작성일</th>
      <td><%= c.created %></td>
    </tr>

    <tr>
      <th>내용</th>
      <td><pre><%= c.content %></pre></td>
    </tr> 

    <tr>
      <th>첨부파일</th>
      <td>
        <% if (c.file) { %>
          <a href="<%= c.file %>" target="_blank" download>첨부파일 다운로드</a>
        <% } else { %>
          없음
        <% } %>
      </td>
    </tr>
  </table>

  <div style="margin-top:20px;">
    <a href="/admin" class="btn">목록으로</a>
    <a href="/delete/<%= c.id %>" class="btn danger" onclick="return confirm('삭제하시겠습니까?')">
      삭제하기
    </a>
  </div>

</div>

</body>
</html>
