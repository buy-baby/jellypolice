<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <title>유저 관리</title>
  <link rel="stylesheet" href="/css/admin.css" />
  <link rel="stylesheet" href="/css/admin_header.css">
</head>
<body>
  <div class="users-container">
    <h1 class="users-title">유저 관리</h1>
    <div class="users-table-wrap">
      <table class="users-table">
        <thead>
          <tr>
            <th>ID</th>
            <th>닉네임</th>
            <th>아이디</th>

            <!-- ✅ 추가 -->
            <th>디스코드 ID</th>
            <th>디스코드 닉네임</th>
            <th>닉네임 갱신일</th>

            <th>권한</th>
            <th>변경</th>
          </tr>
        </thead>

        <tbody>
          <% (users || []).forEach(u => { %>
            <tr>
              <td><%= u.id %></td>
              <td><%= u.nickname %></td>
              <td><%= u.username %></td>

              <td class="mono"><%= u.discord_id || "-" %></td>
              <td><%= u.discord_name || "-" %></td>
              <td><%= (u.discord_last_verified_at || "").slice(0,10) || "-" %></td>

              <td><b><%= u.role %></b></td>

              <td>
                <form method="POST" action="/admin/users/<%= u.id %>/role" class="users-role-form">
                  <select name="role" class="users-role-select">
                    <option value="user" <%= u.role === 'user' ? 'selected' : '' %>>user</option>
                    <option value="admin" <%= u.role === 'admin' ? 'selected' : '' %>>admin</option>
                  </select>
                  <button type="submit" class="users-save-btn">저장</button>
                </form>
              </td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
  </div>
</body>
</html>
