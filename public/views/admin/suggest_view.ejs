<!doctype html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <title>건의 상세</title>
  <link rel="stylesheet" href="/css/admin.css">
  <link rel="stylesheet" href="/css/admin_header.css">
</head>
<body>
  <%- include("../partials/admin_header") %>

  <div class="detail-title-row">
    <h1>📝 건의 상세</h1>
  </div>

  <div class="detail-actions">
    <a class="back-btn" href="/admin/suggest">← 목록으로</a>
  </div>

  <hr class="detail-divider" />

  <div class="detail-section-title">작성자 계정 정보</div>

  <div class="detail-mini-box">
    <div class="detail-kv">
      <div class="detail-k">계정 번호 :</div>
      <div class="detail-v"><%= (s.userId ?? "-") %></div>

      <div class="detail-k">이름 :</div>
      <div class="detail-v"><%= s.name || "-" %></div>

      <div class="detail-k">고유번호 :</div>
      <div class="detail-v"><%= s.identity || "-" %></div>

      <div class="detail-k">아이디 :</div>
      <div class="detail-v"><%= s.username || "-" %></div>
    </div>
  </div>

  <hr class="detail-divider" />

  <div class="detail-section-title">건의 내용</div>

  <div class="detail-content-wrap">
    <div class="info-box">
      <p><b>이름:</b> <%= s.name || "-" %></p>
      <p><b>고유번호:</b> <%= s.identity || "-" %></p>

      <p><b>내용:</b></p>
      <div class="content-box"><%= s.content || "" %></div>

      <p style="margin-top:16px;"><b>제출일:</b> <%= (s.created || "").slice(0,10) %></p>
    </div>
  </div>
</body>
</html>
