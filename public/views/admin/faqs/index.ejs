<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>FAQ 관리</title>
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="/css/admin_header.css">
  <style>
    .admin-wrap{ max-width: 1100px; margin: 24px auto; padding: 0 18px; }
    .admin-card{ background:#fff; border:1px solid rgba(0,0,0,.1); border-radius:14px; padding:18px; box-shadow:0 10px 22px rgba(0,0,0,.06); }
    .admin-title{ font-size:24px; font-weight:900; margin:0 0 12px; }
    .admin-grid{ display:grid; grid-template-columns: 1fr 1fr; gap: 14px; }
    .admin-grid textarea{ min-height: 140px; resize: vertical; }
    .admin-grid input, .admin-grid textarea{
      width:100%; box-sizing:border-box; padding:12px 12px; border-radius:10px;
      border:1px solid rgba(0,0,0,.15);
      font-family: inherit;
    }
    .admin-actions{ margin-top: 12px; display:flex; gap:10px; }
    .btn{
      padding: 10px 14px; border-radius:10px; border:0; cursor:pointer;
      font-weight:900; text-decoration:none; display:inline-flex; align-items:center; justify-content:center;
    }
    .btn-primary{ background:#1a4bd9; color:#fff; }
    .btn-ghost{ background:#eef2f9; color:#0b3fa7; }
    .list{ margin-top: 18px; display:flex; flex-direction:column; gap:10px; }
    .row{
      background:#f7fbff; border:1px solid rgba(0,0,0,.12); border-radius:12px;
      padding: 12px 12px;
    }
    .row-top{ display:flex; align-items:center; justify-content:space-between; gap:10px; }
    .row-title{ font-weight:900; color:#0b3fa7; }
    .row-content{ margin-top:8px; color:#222; opacity:.9; line-height:1.55; white-space: pre-wrap; }
    .row-btns{ display:flex; gap:8px; }
    .btn-danger{ background:#ffebee; color:#b71c1c; }
  </style>
</head>
<body class="page-admin">
  <%- include("../../partials/header") %>

  <div class="admin-wrap">
    <div class="admin-card">
      <h1 class="admin-title">FAQ 관리</h1>

      <form action="/admin/faqs" method="POST">
        <div class="admin-grid">
          <div>
            <label><b>제목</b></label>
            <input name="title" placeholder="FAQ 제목을 입력하세요" required />
          </div>
          <div>
            <label><b>내용</b></label>
            <textarea name="content" placeholder="FAQ 내용을 입력하세요" required></textarea>
          </div>
        </div>

        <div class="admin-actions">
          <button class="btn btn-primary" type="submit">추가</button>
          <a class="btn btn-ghost" href="/">메인으로</a>
        </div>
      </form>

      <div class="list">
        <% if (!faqs || faqs.length === 0) { %>
          <div class="row">등록된 FAQ가 없습니다.</div>
        <% } else { %>
          <% faqs.forEach(f => { %>
            <div class="row">
              <div class="row-top">
                <div class="row-title"><%= f.title %></div>
                <div class="row-btns">
                  <a class="btn btn-ghost" href="/admin/faqs/<%= f.id %>/edit">수정</a>
                  <form action="/admin/faqs/<%= f.id %>/delete" method="POST" style="margin:0;">
                    <button class="btn btn-danger" type="submit" onclick="return confirm('삭제할까요?')">삭제</button>
                  </form>
                </div>
              </div>
              <div class="row-content"><%= f.content %></div>
            </div>
          <% }) %>
        <% } %>
      </div>
    </div>
  </div>
</body>
</html>
