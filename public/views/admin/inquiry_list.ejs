<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <title>민원 목록</title>
  <link rel="stylesheet" href="/css/admin.css">
</head>

<body>

  <%- include("../partials/admin_header") %>  
  <div class="admin-container">
    <h1>📄 민원 목록</h1>
    <a href="/admin" class="back-btn">← 관리자 메인</a>

    <table class="admin-table">
      <tr>
        <th>ID</th>
        <th>이름</th>
        <th>고유번호</th>
        <th>날짜</th>
        <th>보기</th>
      </tr>

      <% complaints.forEach(c => { %>
        <tr>
          <td><%= c.id %></td>
          <td><%= c.name %></td>
          <td><%= c.identity %></td>
          <td><%= c.created.substring(0,10) %></td>
          <td><a class="view-btn" href="/admin/inquiry/view/<%= c.id %>">보기</a></td>
        </tr>
      <% }) %>
    </table>
  </div>
</body>
</html>
