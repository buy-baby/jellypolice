<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>젤리 경찰청</title>

  <style>
    body {
      font-family: "Noto Sans KR", sans-serif;
      margin: 0;
      background: #f6f9ff;
    }

    /* ========== 헤더 ========== */
    header {
      display: flex;
      align-items: center;
      padding: 16px 24px;
      background: #003c8f;
      color: white;
    }
    header img {
      width: 85px;
      margin-right: 18px;
    }
    header h1 {
      margin: 0;
      font-size: 26px;
    }

    /* ========== 메뉴 바 ========== */
    .menu {
      width: 100%;
      background: #1a4bd9;
      padding: 16px 0;
      display: flex;
      justify-content: center;
      gap: 90px;
      position: relative;
    }

    .menu a {
      color: white;
      font-size: 18px;
      font-weight: bold;
      text-decoration: none;
      cursor: pointer;
      user-select: none;
    }

    .arrow {
      font-size: 14px;
      margin-left: 4px;
    }

    /* ========== Mega Menu 컨테이너 ========== */
    .mega-dropdown {
      position: relative;
    }

    .mega-menu {
      display: none;
      position: absolute;
      top: 100%;
      left: 50%;
      transform: translate(-50%, -20px);
      width: 70%;
      background: white;
      border-radius: 12px;
      padding: 25px 35px;
      box-shadow: 0 8px 25px rgba(0,0,0,0.15);
      z-index: 999;

      opacity: 0;
      transition: opacity 0.25s ease, transform 0.25s ease;
    }

    /* 열렸을 때 */
    .mega-menu.show {
      display: block;
      opacity: 1;
      transform: translate(-50%, 0);
    }

    /* 제목 */
    .mega-title {
      font-size: 20px;
      font-weight: bold;
      margin-bottom: 18px;
      color: #0b3fa7;
    }

    /* 카드 정렬 */
    .mega-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 25px;
    }

    /* 카드 스타일 */
    .mega-card {
      background: #eef2f9;
      border-radius: 12px;
      padding: 18px;
      transition: 0.2s;
      cursor: pointer;
    }
    .mega-card:hover {
      background: #dbe5f5;
      transform: translateY(-3px);
    }
    .mega-card h4 {
      margin: 0 0 10px;
      color: #1a4bd9;
    }
    .mega-card p {
      margin: 0;
      font-size: 14px;
      color: #333;
    }
  </style>
</head>

<body>

  <!-- ========== 상단 헤더 ========== -->
  <header>
    <img src="/logo2.png" alt="로고">
    <h1>젤리 경찰청</h1>
  </header>

  <!-- ========== 메뉴 ========== -->
  <nav class="menu">

    <!-- 소개 -->
    <div class="mega-dropdown">
      <a class="menu-btn">소개 <span class="arrow">▼</span></a>

      <div class="mega-menu">
        <div class="mega-title">소개</div>

        <div class="mega-grid">
          <div class="mega-card" onclick="location.href='/intro/agency'">
            <h4>경찰청 소개</h4>
            <p>젤리 경찰청의 역할과 기능</p>
          </div>

          <div class="mega-card" onclick="location.href='/intro/rank'">
            <h4>직급표</h4>
            <p>경찰 계급 및 구조 안내</p>
          </div>

          <div class="mega-card" onclick="location.href='/intro/department'">
            <h4>부서 소개</h4>
            <p>각 부서의 업무와 역할 설명</p>
          </div>
        </div>
      </div>
    </div>

    <!-- 채용공고 -->
    <div class="mega-dropdown">
      <a class="menu-btn">채용공고 <span class="arrow">▼</span></a>

      <div class="mega-menu">
        <div class="mega-title">채용공고</div>

        <div class="mega-grid">
          <div class="mega-card" onclick="location.href='/apply/conditions'">
            <h4>지원 자격</h4>
            <p>지원 가능한 조건 안내</p>
          </div>

          <div class="mega-card" onclick="location.href='/apply/apply'">
            <h4>지원서 작성</h4>
            <p>온라인 지원서를 작성하세요</p>
          </div>
        </div>
      </div>
    </div>

    <!-- 고객센터 -->
    <div class="mega-dropdown">
      <a class="menu-btn">고객센터 <span class="arrow">▼</span></a>

      <div class="mega-menu">
        <div class="mega-title">고객센터</div>

        <div class="mega-grid">
          <div class="mega-card" onclick="location.href='/inquiry'">
            <h4>민원실</h4>
            <p>민원을 접수하고 처리 현황 확인</p>
          </div>

          <div class="mega-card" onclick="location.href='/suggest'">
            <h4>건의실</h4>
            <p>개선 의견을 전달할 수 있습니다</p>
          </div>
        </div>
      </div>
    </div>

  </nav>

  <!-- 중앙 빈 공간 -->
  <div style="height: 500px;"></div>

  <!-- ========== 드롭다운 JS ========== -->
  <script>
    document.querySelectorAll(".mega-dropdown").forEach(drop => {
      const btn = drop.querySelector(".menu-btn");
      const menu = drop.querySelector(".mega-menu");
      const arrow = drop.querySelector(".arrow");

      btn.addEventListener("click", (e) => {
        e.preventDefault();

        // 이미 열려 있으면 닫기
        if (menu.classList.contains("show")) {
          menu.classList.remove("show");
          arrow.textContent = "▼";
          return;
        }

        // 다른 메뉴들 닫기
        document.querySelectorAll(".mega-menu.show").forEach(m => {
          m.classList.remove("show");
          m.parentElement.querySelector(".arrow").textContent = "▼";
        });

        // 현재 메뉴 열기
        menu.classList.add("show");
        arrow.textContent = "▲";
      });
    });

    // 화면 클릭하면 메뉴 닫기
    document.addEventListener("click", (e) => {
      if (!e.target.closest(".mega-dropdown")) {
        document.querySelectorAll(".mega-menu.show").forEach(m => {
          m.classList.remove("show");
          m.parentElement.querySelector(".arrow").textContent = "▼";
        });
      }
    });
  </script>

</body>
</html>
