<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <title>로그인</title>
  <link rel="stylesheet" href="/css/style.css" />
  <link rel="stylesheet" href="/css/header.css">
  <link rel="stylesheet" href="/css/auth.css">
</head>
<body>
  <%- include("../partials/header") %>

  <div class="auth-wrap">
    <h1 class="auth-title">로그인</h1>

    <% if (error) { %>
      <div class="auth-error"><%= error %></div>
    <% } %>

    <form method="POST" action="/login" class="auth-form">
      <input type="hidden" name="nextUrl" value="<%= nextUrl || '/' %>" />
      <input name="username" placeholder="아이디" class="auth-input" />
      <input name="password" type="password" placeholder="비밀번호" class="auth-input" />

      <button type="submit" class="auth-btn-primary">로그인</button>
    </form>

    <div style="margin-top:14px;">
      <a href="/register" class="auth-link">계정이 없어요 → 회원가입</a>
    </div>
  </div>

  <script>localStorage.removeItem("jp_register_draft_v1");</script>
</body>
</html>
